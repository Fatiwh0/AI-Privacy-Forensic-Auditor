project:
  name: AI Privacy Forensic Auditor
  language: english
  timezone: UTC

reproducibility:
  random_seed: 42
  n_jobs: -1

run:
  save_artifacts: true
  save_figures: true
  verbose: true

outputs:
  base_dir: outputs
  figures_dir: outputs/figures
  tables_dir: outputs/tables
  models_dir: outputs/models
  reports_dir: outputs/reports
